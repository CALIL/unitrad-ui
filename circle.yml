machine:
  node:
    version: 6.9.1
test:
  override:
    - mocha test --reporter mocha-junit-reporter --require babel-core/register:
        environment:
          MOCHA_FILE: $CIRCLE_TEST_REPORTS/junit/test-results.xml
